# JWT Secret - Generate a strong random secret for production
# Example: openssl rand -base64 32
JWT_SECRET=your-secret-key-here-change-in-production

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=autohost
DB_PASSWORD=autohost
DB_NAME=autohost_cloud
DB_SSLMODE=disable

# Server
PORT=8080
